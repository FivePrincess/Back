spring:
  datasource:
    url: jdbc:mysql://localhost:3306
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  jwt:
    secret: guzip_secret_key_slkdfjaiwefskdflskjdiwejfsldkjfwied

server:
#  port: ${PORT}
#


logging:
  level:
    root: info
    org.springframework.web: debug

